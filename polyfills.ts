// Polyfill for the TextEncoder.encode() API, needed for `viem`.
// Ideally we shouldn't need to manually import this, and should be resolved directly inside Web3Modal
import 'fastestsmallesttextencoderdecoder';
